# LLM 提供商: openai, tongyi, glm, ollama
LLM_PROVIDER=tongyi

# 语音识别器: faster-whisper (默认) 或 funasr (中文更优)
ASR_PROVIDER=faster-whisper
# Whisper 本地/在线二选一：
# 1) 离线本地路径：指向已下载的本地模型目录
# WHISPER_MODEL=/Users/wuh/models/faster-whisper-medium
# HF_HUB_OFFLINE=1
# 2) 走代理/镜像在线下载：
# WHISPER_MODEL=medium
# HF_HUB_OFFLINE=0
# HTTP_PROXY=
# HTTPS_PROXY=
# HF_ENDPOINT=https://hf-mirror.com

# RAG 提供商: faiss (默认) 或 chromadb
RAG_PROVIDER=faiss

# chromadb 持久化配置
CHROMA_DIR=data/chroma

# Ollama 配置 (本地运行，无需 Key)
# 8G 内存建议使用 1.5b 模型: ollama run qwen2:1.5b
# 下载地址: https://ollama.com
OLLAMA_BASE_URL=http://localhost:11434

# OpenAI 配置 (如果 LLM_PROVIDER=openai)
OPENAI_API_KEY=
OPENAI_BASE_URL=

# 通义千问 (DashScope)
# 在这里填入你的 Key: https://dashscope.console.aliyun.com/apiKey
DASHSCOPE_API_KEY=sk-b7c1ff7027484902b0c764b2c80dbe2d

# 智谱 AI (如果 LLM_PROVIDER=glm)
ZHIPUAI_API_KEY=

# Whisper 配置
# 本地模型大小: tiny, base, small, medium, large-v3
WHISPER_MODEL_SIZE=tiny
# 索引来源: minutes(纪要) 或 transcript(原文)
INDEX_SOURCE=minutes


# 是否使用 LangChain 生成向量
USE_LANGCHAIN=True
